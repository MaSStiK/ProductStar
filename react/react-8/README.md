# Оффлайн чат
Одностраничное приложение чата, созданное на **React** с использованием **Redux**, **@reduxjs/toolkit** и **react-virtualized** для оптимизированного отображения большого количества сообщений.  
Данные пользователя и сообщения сохраняются в Redux и автоматически кешируются в `localStorage`, позволяя восстановить сеанс после перезагрузки страницы.

Приложение поддерживает аватарки пользователей, которые генерируются автоматически на основе имени с использованием публичного API `Dicebear`. Каждому пользователю присваивается уникальный аватар, что улучшает визуальное восприятие чата и помогает легче ориентироваться в переписке.

---

## 📌 Возможности
- Регистрация и авторизация пользователей.
- Автоматический вход при наличии данных в `localStorage`.
- Отправка и отображение сообщений в реальном времени.
- Виртуализация списка сообщений для высокой производительности.
- Кеширование чата и состояния пользователя.

---

## 📄 Страницы приложения

### 1. **Главная (чат)**
<img width="1449" height="932" alt="Скриншот сделанный 2025-12-06 в 02 10 19-fullpage" src="https://github.com/user-attachments/assets/b9cc8d74-8865-440e-b9fe-126c12ec1ff1" />
<img width="1449" height="932" alt="Скриншот сделанный 2025-12-06 в 02 10 57-fullpage" src="https://github.com/user-attachments/assets/5d5526b0-ae1f-479e-b68b-6ea9a735e5d9" />
<img width="1449" height="932" alt="Скриншот сделанный 2025-12-06 в 02 13 55-fullpage" src="https://github.com/user-attachments/assets/1ed00bb2-3d82-4b6d-9d0c-b06de738b565" />

### 2. **Страница входа**
<img width="1449" height="932" alt="Скриншот сделанный 2025-12-06 в 02 10 30-fullpage" src="https://github.com/user-attachments/assets/403d7bb7-74e6-4b01-bf43-06dc6be6c32a" />

### 3. **Страница регистрации**
<img width="1449" height="932" alt="Скриншот сделанный 2025-12-06 в 02 10 32-fullpage" src="https://github.com/user-attachments/assets/ee616aa3-1335-4ee8-8585-4c292e9d0aca" />

---

## 📦 Установка и запуск
Приложение состоит из frontend и backend части, поэтому установка выполняется в два шага.

1. **Устанавливаем зависимости фронтенда:**
```bash
npm i
npm start
```

2. **Переходим в backend и устанавливаем зависимости:**
```bash
cd ./homework-backend
npm i
npm start
```

После запуска:
- Backend работает локально как API-сервер и доступен по ссылке `http://localhost:3001`.
- Frontend автоматически подключается к нему и отображает данные.

---

## 🔐 Авторизация и хранение данных
- Все данные приложения хранятся в Redux.
- При изменениях состояния они синхронно записываются в `localStorage`.
- При загрузке приложения Redux восстанавливает данные из хранилища.
- Если найден токен — пользователь автоматически авторизуется.

---

## ⚡ Виртуализация чата
Все сообщения отображаются через: `react-virtualized List + AutoSizer`
Это позволяет:
- рендерить только видимые элементы;
- не нагружать DOM при большом числе сообщений;
- оптимизировать производительность.

---

## 🔄 Автоматическая синхронизация сообщений
Каждые 1000 миллисекунд отправляется запрос на получение свежих сообщений.  
При появлении новых — список обновляется и (опционально) прокручивается вниз.

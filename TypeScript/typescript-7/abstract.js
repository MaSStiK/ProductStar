"use strict";
class Pet2 {
    constructor(typeOfPet) {
        this.typeOfPet = typeOfPet;
    }
}
// Абстракция класса, то есть он не будет использоваться так как он сейчас написан, то есть мы будем думать над его реализацией позже
class People2 {
    constructor(name, flat) {
        this.name = name;
        this.flat = flat;
    }
}
